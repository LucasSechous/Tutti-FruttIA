spring.application.name=tutti-fruttia

# ================================
# ? CONFIGURACIÓN DE SUPABASE (POSTGRES)
# ================================
spring.datasource.url=jdbc:postgresql://db.vvbfbfcwhypwcvmwgugv.supabase.co:5432/postgres?sslmode=require
spring.datasource.username=postgres
spring.datasource.password=4EiPlc9Ai6dbEvZP
spring.datasource.driver-class-name=org.postgresql.Driver

# ================================
# ?? JPA / HIBERNATE
# ================================
# Spring 6 / Hibernate 6 ya detectan bien el dialecto, pero esto no molesta
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Mientras estés ajustando el modelo, "update" viene bien.
# Cuando esté estable, podés cambiarlo a "validate" para no tocar más el schema.
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ================================
# ? IMPORTANTE
# No desactivar la configuración del DataSource
# ================================
# (NO AGREGAR) spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
